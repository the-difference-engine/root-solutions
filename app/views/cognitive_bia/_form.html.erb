<%= form_for(cognitive_bium, :html => { :role => 'form'}) do |f| %>
  <% if cognitive_bium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cognitive_bium.errors.count, "error") %> prohibited this cognitive_bium from being saved:</h2>

      <ul>
      <% cognitive_bium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class:"form-control" %>
  </div>

  <div class="actions">
    <%= f.submit ( f.object.new_record? ? "Create" : "Update"), class: "btn-primary btn"  %>
  </div>
<% end %>
