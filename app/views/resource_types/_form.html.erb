<%= form_for(resource_type, :html => { :role => 'form'}) do |f| %>
  <% if resource_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resource_type.errors.count, "error") %> prohibited this resource_type from being saved:</h2>

      <ul>
      <% resource_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class:"form-control" %>
  </div>

  <div class="actions">
    <%= f.submit ( f.object.new_record? ? "Create" : "Update"), class: "btn-primary btn"  %>
  </div>
<% end %>
